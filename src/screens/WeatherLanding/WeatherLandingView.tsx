import { ScrollView, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export const WeatherLandingView = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text style={{ paddingTop: 0 }}>{'LOL'}</Text>
    </View>
  );
};
